#pragma once

#include <qstring.h>

const float ZOOM_VALUE = 0.5f;
const float ZOOM_MAX = 3.0f;
const float ZOOM_MIN = 0.5f;


const int FRAME_PER_SECOND = 30;

const double DEFAULT_BRIGHTNESS = 0.00;
const double DEFAULT_CONTRAST = 1.00;

const int DEFAULT_STRESS_RADIUS = 300;
const int DEFAULT_STRESS_SAMPLES = 5;
const int DEFAULT_STRESS_INTERATIONS = 5;
const bool DEFAULT_STRESS_ENHANCE_SHADOWS = false;

const bool DEFAULT_STRETCH_CONTRAST_KEEP_COLORS = true;
const bool DEFAULT_STRETCH_CONTRAST_PERCEPTUAL = false;

const int DEFAULT_CONTRAST_CURVE_SAMPLING_PLINTS = 0;


const int FPS_RECROD_VIDEO = 30;

const QString PATH_CAPTURE_FRAME = "/captures";
const QString PATH_RECORD_VIDEO = "/videos";

const QString MENU_PLAY = "Play";
const QString MENU_PAUSE = "Pause";

